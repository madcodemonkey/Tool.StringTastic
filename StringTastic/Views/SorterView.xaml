<UserControl x:Class="StringTastic.Views.SorterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFF0F0F0" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFE0E0E0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <Button Name="ClearButton" MinHeight="25" Margin="5" MinWidth="40" 
                    Command="{Binding RichTextBoxCommon.ClearRichTextBoxCommand}" CommandParameter="{Binding ElementName=RtbSort}">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconClear}" Margin="0,0,4,0" />
                    <TextBlock Text="Clear" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Name="UndoButton" MinHeight="25" Margin="5" MinWidth="40" 
                    Command="ApplicationCommands.Undo" CommandTarget="{Binding ElementName=RtbSort}" ToolTip="Undo last action">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconUndo}" Margin="0,0,4,0" />
                    <TextBlock Text="Undo" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Name="SortAscendingButton" MinHeight="25" Margin="5" MinWidth="100" Click="SortAscendingButton_Click" ToolTip="Sort lines in ascending order">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconSort}" Margin="0,0,4,0" />
                    <TextBlock Text="Sort Ascending" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Name="SortDescendingButton" MinHeight="25" Margin="5" MinWidth="100" Click="SortDescendingButton_Click" ToolTip="Sort lines in descending order">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconSort}" Margin="0,0,4,0" />
                    <TextBlock Text="Sort Descending" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Name="UniqueButton" MinHeight="25" Margin="5" MinWidth="80" Click="UniqueButton_Click" ToolTip="Remove duplicate lines">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconUnique}" Margin="0,0,4,0" />
                    <TextBlock Text="Unique" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Name="TrimButton" MinHeight="25" Margin="5" MinWidth="80" Click="TrimButton_Click" ToolTip="Trim whitespace from each line">
                <StackPanel Orientation="Horizontal">
                    <ContentControl ContentTemplate="{StaticResource IconTrim}" Margin="0,0,4,0" />
                    <TextBlock Text="Trim" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- RichTextBox -->
        <RichTextBox Name="RtbSort" Grid.Row="1" Style="{DynamicResource RichTextWithBetterSpacing}" 
                      HorizontalAlignment="Stretch" Margin="5"  VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Auto">
            <RichTextBox.ContextMenu>
                <ContextMenu >
                    <MenuItem Command="ApplicationCommands.SelectAll" />
                    <MenuItem Command="ApplicationCommands.Copy" />
                    <MenuItem Command="ApplicationCommands.Cut" />
                    <MenuItem Command="ApplicationCommands.Paste" />
                    <Separator />
                    <MenuItem Header="_Load">
                        <MenuItem Header="_Contents of a file" 
                                      Command="{Binding RichTextBoxCommon.LoadFileContentsCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}},Path=PlacementTarget}" />
                        <MenuItem Header="File _names from a directory"
                                      Command="{Binding RichTextBoxCommon.LoadFileNamesCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}},Path=PlacementTarget}" />
                        <MenuItem Header="Folder _names from a directory"
                                      Command="{Binding RichTextBoxCommon.LoadFolderNamesCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}},Path=PlacementTarget}" />
                    </MenuItem>
                </ContextMenu>
            </RichTextBox.ContextMenu>
        </RichTextBox>
    </Grid>
</UserControl>

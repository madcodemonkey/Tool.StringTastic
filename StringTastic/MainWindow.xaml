<Window x:Class="StringTastic.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:StringTastic.Views"
        Title="StringTastic" Height="762.344" Width="1047.656" WindowStartupLocation="CenterScreen"
        Icon="{StaticResource AppIcon}"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    <Window.Resources>
        <Style x:Key="RichTextWithBetterSpacing" TargetType="{x:Type RichTextBox}">
            <Style.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </Style.Resources>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="{DynamicResource RichTextBoxBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource RichTextBoxForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource RichTextBoxBorderBrush}"/>
            <!-- Performance improvements -->
            <Setter Property="SpellCheck.IsEnabled" Value="False"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <!-- Small, flat tab close button style (vector X for reliable rendering) -->
        <Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTip" Value="Close tab" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="2">
                            <Viewbox Width="12" Height="12" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Canvas Width="12" Height="12">
                                    <Path x:Name="p1" Data="M2,2 L10,10" Stroke="{DynamicResource TabCloseButtonStrokeBrush}" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                    <Path x:Name="p2" Data="M10,2 L2,10" Stroke="{DynamicResource TabCloseButtonStrokeBrush}" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                                </Canvas>
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource TabCloseButtonHoverBackgroundBrush}" />
                                <Setter TargetName="p1" Property="Stroke" Value="{DynamicResource TabCloseButtonHoverStrokeBrush}" />
                                <Setter TargetName="p2" Property="Stroke" Value="{DynamicResource TabCloseButtonHoverStrokeBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource TabCloseButtonPressedBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="p1" Property="Stroke" Value="{DynamicResource TabCloseButtonDisabledStrokeBrush}" />
                                <Setter TargetName="p2" Property="Stroke" Value="{DynamicResource TabCloseButtonDisabledStrokeBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Raised-looking toolbar button style: visible border, corner radius and hover/pressed colors -->
        <Style x:Key="ToolbarRaisedButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource ButtonHoverBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ButtonPressedBackgroundBrush}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource ButtonPressedBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ButtonDisabledBackgroundBrush}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource ButtonDisabledBorderBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource ButtonDisabledForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Apply the raised style to all toolbar buttons by overriding the ToolBar button style key -->
        <Style TargetType="Button" x:Key="{x:Static ToolBar.ButtonStyleKey}" BasedOn="{StaticResource ToolbarRaisedButton}" />

    </Window.Resources>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" 
              Background="{DynamicResource MenuBackgroundBrush}"
              Foreground="{DynamicResource MenuForegroundBrush}">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Click="Exit_Click">
                    <MenuItem.Icon>
                        <ContentControl ContentTemplate="{StaticResource IconExit}" Width="16" Height="16" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Theme">
                    <MenuItem Header="_Light" Click="ThemeLight_Click" IsCheckable="True" x:Name="MenuItemLightTheme"  />
                    <MenuItem Header="_Dark" Click="ThemeDark_Click" IsCheckable="True" x:Name="MenuItemDarkTheme" />
                    <MenuItem Header="_Battleship Gray" Click="ThemeBattleshipGray_Click" IsCheckable="True" x:Name="MenuItemBattleshipGrayTheme" IsChecked="True" />
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!-- Main content area with left and right containers -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Container with search box and tool list -->
            <Border Grid.Column="0" 
                    BorderBrush="{DynamicResource BorderBrush}" 
                    BorderThickness="0,0,1,0" 
                    Padding="10"
                    Background="{DynamicResource PanelBackgroundBrush}">
                <DockPanel>
                    <!-- Search Box -->
                    <TextBox x:Name="SearchTextBox" 
                             DockPanel.Dock="Top" 
                             Padding="5" 
                             Margin="0,0,0,10"
                             TextChanged="SearchTextBox_TextChanged"
                             Background="{DynamicResource TextBoxBackgroundBrush}"
                             Foreground="{DynamicResource TextBoxForegroundBrush}"
                             BorderBrush="{DynamicResource TextBoxBorderBrush}">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="{DynamicResource TextBoxForegroundBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Search tools..." 
                                                                   Foreground="{DynamicResource TextBoxPlaceholderBrush}" 
                                                                   Margin="5,0,0,0" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Tools List -->
                    <ListBox x:Name="ToolsListBox" 
                             BorderThickness="0"
                             SelectionChanged="ToolsListBox_SelectionChanged"
                             Background="{DynamicResource ListBoxBackgroundBrush}"
                             Foreground="{DynamicResource ListBoxForegroundBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                    <ContentControl ContentTemplate="{Binding IconTemplate}" 
                                                    Width="16" 
                                                    Height="16" 
                                                    Margin="0,0,8,0" />
                                    <TextBlock Text="{Binding DisplayName}" 
                                               VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          Width="5" 
                          HorizontalAlignment="Stretch" 
                          VerticalAlignment="Stretch" 
                          Background="{DynamicResource SplitterBrush}"
                          ShowsPreview="False" />

            <!-- Right Container with TabControl -->
            <TabControl x:Name="MainTabControl" 
                        Grid.Column="2"
                        Background="{DynamicResource TabControlBackgroundBrush}" />
        </Grid>
    </DockPanel>

</Window>

